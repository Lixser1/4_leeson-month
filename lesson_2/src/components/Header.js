import React from 'react';
import Title from "./Title";

const Header = () => {
    return (
        <div>
            <Title title={"Header"} name={"Footer"}/>
        </div>
    );
};

export default Header;